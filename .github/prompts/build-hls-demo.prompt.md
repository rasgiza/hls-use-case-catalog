---
name: 'Build HLS Demo'
description: 'Run the complete research, implementation, and independent QA workflow for one self-contained catalog demo.'
argument-hint: 'Use-case title, ID, or #uc route'
agent: hls-demo-studio
---

Use the `build-hls-demo` skill to create exactly one self-contained artifact. Resolve the catalog record, prepare the brief, implement the demo, run executable checks, and hand it to independent QA. Do not batch use cases or broaden into catalog-shell changes.